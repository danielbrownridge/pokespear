from setuptools import setup, find_packages
setup(
    name="pokespear",
    package=[
        ""
    ],
    package_dir={
        "": "src"
    },
    use_scm_version=True,
    setup_requires=[
        "setuptools_scm",
    ]
)
